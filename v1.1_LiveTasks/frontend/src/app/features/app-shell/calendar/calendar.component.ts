import { Component } from '@angular/core';

@Component({
  selector: 'app-calendar',
  standalone: true,
  template: `
    <h2 class="text-2xl font-bold text-primary">Calendario</h2>
    <p class="text-secondary mt-2">Sección de calendario — se implementará en una fase posterior.</p>
  `,
})
export class CalendarComponent {}
