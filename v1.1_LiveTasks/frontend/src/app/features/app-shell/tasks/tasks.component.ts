import { Component } from '@angular/core';

@Component({
  selector: 'app-tasks',
  standalone: true,
  template: `
    <h2 class="text-2xl font-bold text-primary">Tareas</h2>
    <p class="text-secondary mt-2">Sección de tareas — se implementará en una fase posterior.</p>
  `,
})
export class TasksComponent {}
